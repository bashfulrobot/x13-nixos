# Repo is a crapshoot right now

```shell
# https://github.com/the-nix-way/home-manager-config-template
nix flake new \
--template github:the-nix-way/home-manager-config-template \
./my-home-manager-config
```
